# ==========================================
# LEGACY INCLUDE FORCE_MOVE — EDDY
# ==========================================
# Назначение:
# - Оставлен как безопасный no-op для старых `local_overrides.cfg`.
# - Runtime `[force_move]` теперь объявлен в `probe_eddy_duo.cfg`.
# Контур:
# - compatibility/read-only: не объявляет секций Klipper и не меняет runtime.

# Блок 1: Совместимость со старыми локальными include.
# Пусто намеренно: дублировать `[force_move]` нельзя.
