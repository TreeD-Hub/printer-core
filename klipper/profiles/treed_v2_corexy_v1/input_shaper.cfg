# ==========================================
# INPUT SHAPER — TreeD V2 CoreXY V1
# ==========================================
# Назначение:
# - Включает модуль input_shaper, чтобы SHAPER_CALIBRATE мог применять
#   результаты сразу в текущей сессии Klipper.
#
# Важно:
# - Частоты, типы и damping_ratio живут в SAVE_CONFIG-блоке printer.cfg.
# - Не задавать shaper_freq/type здесь: иначе SAVE_CONFIG не сможет
#   сохранить результаты SHAPER_CALIBRATE без конфликта с include.
# - Этот блок не зависит от Octopus Pro.
# Контур:
# - required: профиль держит секцию input_shaper, а значения приходят из autosave.

# Блок 1: Пустая секция модуля input shaper.
[input_shaper]
