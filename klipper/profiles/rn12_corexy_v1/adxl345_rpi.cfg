# ==========================================
# LEGACY SHIM: ADXL345 ЧЕРЕЗ RASPBERRY PI
# ==========================================
# Назначение:
# - Сохраняет обратную совместимость со старым путём include.
# - Перенаправляет на legacy-конфиг без изменения его содержимого.
#
# Контур:
# - read-only shim, не подключается в активном printer.cfg по умолчанию.

[include legacy/adxl345_rpi.cfg]
