# `klipper/generated`

Runtime-generated Klipper config placeholders.

## Contract

- Files in this directory are overwritten in staging/runtime by loader steps.
- Repository copies must not contain real machine UUIDs or serial paths.
- `treed_machine_mcus.cfg` owns `[mcu]`, `[mcu EBBCan]`, and optional `[mcu eddy]`.

